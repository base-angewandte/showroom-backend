version: '2'
services:

  showroom-django:
    build:
      context: ./src
      dockerfile: Dockerfile.dev
    image: showroom-django-dev
    container_name: showroom-django-dev
    ports:
      - "127.0.0.1:8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
